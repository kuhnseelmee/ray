version: '3.8'

services:
  web:
    build: .
    ports:
      - '4000:4000'
    environment:
      - NODE_ENV=production
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - NEXT_PUBLIC_ENABLE_AI=${NEXT_PUBLIC_ENABLE_AI:-true}
    restart: unless-stopped